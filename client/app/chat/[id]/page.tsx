"use client";
/** 匿名聊天页 (/chat/:id) */

export default function ChatPage({ params }: { params: { id: string } }) {
  return <main>{/* ChatHeader + MessageList + MessageInput + ReportButton */}</main>;
}
